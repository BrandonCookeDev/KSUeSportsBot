/** TARGET ENVIRONMENT (test or prod) **/
//var target = "prod";
var target = "test"; //'test' or 'prod'
var version = "1.0.1";
var dcTimeout = 10000; 

/** MODES **/

module.exports = {
	target: target,
	version: version,
	dcTimeout: dcTimeout
};